// Central map of semantic icon names → lucide-react components.
// Import from here rather than directly from lucide-react so the whole app
// stays in sync when an icon needs to be swapped.

export {
  // ── Domains ────────────────────────────────────────────────────────────────
  MessageSquare,   // communication
  Newspaper,       // social
  Clapperboard,   // streaming
  ShoppingBag,    // commerce

  // ── Top-bar / chrome ───────────────────────────────────────────────────────
  Menu,
  Search,
  Bell,
  Sun,
  Moon,
  LogOut,
  X,
  User,
  Settings,
  ChevronRight,
  ChevronDown,
  ChevronLeft,

  // ── Sub-navigation ─────────────────────────────────────────────────────────
  Home,
  Compass,        // Explore
  Globe2,         // Communities / worldwide
  Plus,
  Pencil,

  // ── Status / feedback ──────────────────────────────────────────────────────
  Wifi,
  WifiOff,
  AlertTriangle,
  CheckCircle2,
  Loader2,
  Sparkles,
  ShieldCheck,
  Zap,
  ExternalLink,

  // ── Communication / calls ──────────────────────────────────────────────────
  Phone,
  PhoneOff,
  Video,
  VideoOff,
  Mic,
  MicOff,

  // ── Commerce ───────────────────────────────────────────────────────────────
  ShoppingCart,
  Lock,
  Star,
  SlidersHorizontal, // Filter/Sort

  // ── Sharing / content ──────────────────────────────────────────────────────
  Share2,
  Link2,
  Copy,
  ArrowUpRight,
  CornerUpLeft,  // reply

  // ── Profile / settings ─────────────────────────────────────────────────────
  Camera,
  Mail,
  Shield,
  Eye,
  EyeOff,
  Save,
  RotateCcw,
  Palette,
  Monitor,
} from 'lucide-react';
